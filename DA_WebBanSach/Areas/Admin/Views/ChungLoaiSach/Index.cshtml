@model IEnumerable<DA_WebBanSach.Models.ChungLoaiSach>

@{
    ViewBag.Title = "Chủng Loại Sách";
}

<ul class="breadcrumb">
      <li><a href="@Url.Action("Index","Home")">Quản Lý</a><span class="divider">&raquo;</span></li>
      <li>Chủng Loại Sách</li>
</ul>
<div class="row">
    <div class="span12">
        <div class="widget-block">
            <div class="widget-head">
                <h5>
                    Danh Sách Chủng Loại Sách</h5>
                <div class="widget-control pull-right">
                    <a href="#" data-toggle="dropdown" class="btn dropdown-toggle"><i class="icon-plus"></i>
                        <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li><a href="@Url.Action("Create")"><i class="icon-plus"></i>Thêm Mới Chủng Loại Sách</a></li>
                    </ul>
                </div>
            </div>
            <div class="widget-content">
                <div class="widget-box">
                    <table class="data-tbl-tools table">
                        <thead>
                            <tr>
                                <th>
                                    ID
                                </th>
                                <th>
                                    Tên Chủng Loại
                                </th>
                                <th>
                                    Chức Năng
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ChungLoaiSachID)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TenChungLoai)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Details", "ChungLoaiSach", new { id = item.ChungLoaiSachID })" class="color-icons find_co" title="Chi tiết"></a> 
                                        <a href="@Url.Action("Edit", "ChungLoaiSach", new { id = item.ChungLoaiSachID })" class="color-icons hammer_screwdriver_co" title="Sửa"></a> 
                                        <a href="@Url.Action("Delete", "ChungLoaiSach", new { id = item.ChungLoaiSachID })" class="color-icons cross_co" title="Xóa"></a> 
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
