@model DA_WebBanSach.Models.ChungLoaiSach

@{
    ViewBag.Title = "Sửa Chủng Loại Sách";
}

<ul class="breadcrumb">
    <li><a href="@Url.Action("Index", "Home")">Quản Lý</a><span class="divider">&raquo;</span></li>
    <li><a href="@Url.Action("Index", "ChungLoaiSach")">Chủng Loại Sách</a><span class="divider">&raquo;</span></li>
    <li>Sửa Chủng Loại Sách</li>
</ul>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    <div class="row">
        <div class="span12">
            <div class="nonboxy-widget">
                <div class="widget-head">
                    <h5>
                        <i class="black-icons blocks_images"></i>Sửa Chủng Loại Sách: 
                    </h5>
                </div>
                <div class="widget-content">
                    <div class="widget-box">
                        <div class="form-horizontal well" id="signupform">
                        <fieldset>
                            <div class="control-group">
                                <label class="control-label">
                                    <b>Tên Loại Sách:</b></label>
                                <div class="controls">
                                    @Html.HiddenFor(model => model.ChungLoaiSachID)
                                    @Html.EditorFor(model => model.TenChungLoai)
                                    @Html.ValidationMessageFor(model => model.TenChungLoai)
                                </div>
                            </div>
                        </fieldset>
                        <div class="form-actions">
                            <input type="submit" value="Save" class="btn btn-success" /> 
                            <a href="@Url.Action("Index", "ChungLoaiSach")" class="btn btn-info">Trở Về Trang Trước</a>
                        </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}