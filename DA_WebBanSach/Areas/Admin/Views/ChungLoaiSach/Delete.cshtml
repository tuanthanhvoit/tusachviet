@model DA_WebBanSach.Models.ChungLoaiSach

@{
    ViewBag.Title = "Xóa Loại Sách";
}
<ul class="breadcrumb">
    <li><a href="@Url.Action("Index", "Home")">Quản Lý</a><span class="divider">&raquo;</span></li>
    <li><a href="@Url.Action("Index", "ChungLoaiSach")">Chủng Loại Sách</a><span class="divider">&raquo;</span></li>
    <li>Xóa Chủng Loại Sách</li>
</ul>
    <div class="row">
        <div class="span12">
            <div class="nonboxy-widget">
                <div class="widget-head">
                    <h5>
                        <i class="black-icons blocks_images"></i>Xóa Chủng Loại Sách:
                    </h5>
                </div>
                <div class="widget-content">
                    <div class="widget-box">
                        <div class="form-horizontal well" id="signupform">
                            <fieldset>
                                <div class="control-group">
                                    <label class="control-label">
                                        <b>Tên Chủng Loại Sách:</b></label>
                                    <div class="control-labelb">
                                        @Html.DisplayFor(model => model.TenChungLoai)
                                    </div>
                                </div>
                            </fieldset>
                            @using (Html.BeginForm())
                            {
                                <div class="form-actions">
                                    <input type="submit" value="Xóa" class="btn btn-success" />
                                    <a href="@Url.Action("Index", "ChungLoaiSach")" class="btn btn-info">Trở Về Trang Trước</a>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>